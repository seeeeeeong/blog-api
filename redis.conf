# Memory limit and eviction policy
maxmemory 48mb
maxmemory-policy volatile-ttl

# Persistence: AOF + RDB
appendonly yes
appendfsync everysec

save 900 1
save 300 10
save 60 10000

# Keep persistence data under the mounted volume
dir /data
